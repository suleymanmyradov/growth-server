# Attached ONLY to the auth container (env_file in docker-compose.prod.yml).
# Holds the ES256 private key — auth is the sole issuer of user tokens.
# Generate: ./scripts/gen-jwt-keys.sh
# Keep on one line with literal \n escapes.
JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
