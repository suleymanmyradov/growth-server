// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: user_preferences.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createUserPreferences = `-- name: CreateUserPreferences :one
INSERT INTO user_preferences (
    theme, language, timezone, user_id
) VALUES ($1, $2, $3, $4)
RETURNING user_id, theme, language, timezone, check_in_time, onboarding_completed, created_at, updated_at
`

func (q *Queries) CreateUserPreferences(ctx context.Context, theme string, language string, timezone string, userID uuid.UUID) (UserPreference, error) {
	row := q.db.QueryRow(ctx, createUserPreferences,
		theme,
		language,
		timezone,
		userID,
	)
	var i UserPreference
	err := row.Scan(
		&i.UserID,
		&i.Theme,
		&i.Language,
		&i.Timezone,
		&i.CheckInTime,
		&i.OnboardingCompleted,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteUserPreferences = `-- name: DeleteUserPreferences :exec
DELETE FROM user_preferences WHERE user_id = $1
`

func (q *Queries) DeleteUserPreferences(ctx context.Context, userID uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteUserPreferences, userID)
	return err
}

const getUserPreferences = `-- name: GetUserPreferences :one
SELECT user_id, theme, language, timezone, check_in_time, onboarding_completed, created_at, updated_at
FROM user_preferences
WHERE user_id = $1
`

func (q *Queries) GetUserPreferences(ctx context.Context, userID uuid.UUID) (UserPreference, error) {
	row := q.db.QueryRow(ctx, getUserPreferences, userID)
	var i UserPreference
	err := row.Scan(
		&i.UserID,
		&i.Theme,
		&i.Language,
		&i.Timezone,
		&i.CheckInTime,
		&i.OnboardingCompleted,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateOnboardingCompleted = `-- name: UpdateOnboardingCompleted :one
INSERT INTO user_preferences (user_id, check_in_time, onboarding_completed)
VALUES ($1, COALESCE($2::time, '09:00'::time), $3)
ON CONFLICT (user_id) DO UPDATE
SET check_in_time = COALESCE($2::time, user_preferences.check_in_time),
    onboarding_completed = CASE WHEN $3 THEN true ELSE user_preferences.onboarding_completed END
RETURNING user_id, theme, language, timezone, check_in_time, onboarding_completed, created_at, updated_at
`

// The onboarding_completed flag is a one-way operation: once true, a general
// settings update (e.g. changing check-in time or accountability style) must
// never reset it to false. The protobuf bool field defaults to false when
// omitted, so the CASE expression preserves the existing value when the input
// is false and only flips to true when explicitly requested.
func (q *Queries) UpdateOnboardingCompleted(ctx context.Context, userID uuid.UUID, checkInTime pgtype.Time, onboardingCompleted bool) (UserPreference, error) {
	row := q.db.QueryRow(ctx, updateOnboardingCompleted, userID, checkInTime, onboardingCompleted)
	var i UserPreference
	err := row.Scan(
		&i.UserID,
		&i.Theme,
		&i.Language,
		&i.Timezone,
		&i.CheckInTime,
		&i.OnboardingCompleted,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateUserPreferences = `-- name: UpdateUserPreferences :one
INSERT INTO user_preferences (user_id, theme, language, timezone)
VALUES ($1, $2, $3, $4)
ON CONFLICT (user_id) DO UPDATE
SET theme = EXCLUDED.theme, language = EXCLUDED.language, timezone = EXCLUDED.timezone
RETURNING user_id, theme, language, timezone, check_in_time, onboarding_completed, created_at, updated_at
`

func (q *Queries) UpdateUserPreferences(ctx context.Context, userID uuid.UUID, theme string, language string, timezone string) (UserPreference, error) {
	row := q.db.QueryRow(ctx, updateUserPreferences,
		userID,
		theme,
		language,
		timezone,
	)
	var i UserPreference
	err := row.Scan(
		&i.UserID,
		&i.Theme,
		&i.Language,
		&i.Timezone,
		&i.CheckInTime,
		&i.OnboardingCompleted,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
